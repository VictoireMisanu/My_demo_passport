<%- include('./partials/header', {title: `Modifier ${user.email}`}) -%>
<h1><%- title %></h1>

<p>Email: <%- user.email %></p>

<form method="POST" action="/auth/profile/<%=user.id%>/modify%>" id="form_modify">
    <div>
        <div>
            <label for="email">Email</label>
        </div>
        <div>
            <input type="email" name="email" id="email" value="<%=user.email%>">
        </div>
    </div>
    <div>
        
        <div>
            <label for="password">Password</label>
        </div>
        <div>
            <input type="password" name="password" id="password">
        </div>
        <!-- <div>
            <select name="role" id="role">
                <option disabled>Choisissez un role</option>
                <option value="member" <%=user.role === "member" && "selected"%>>Membre</option>
                <option value="admin" <%=user.role === "admin" && "selected"%>>Admin</option>
            </select>
        </div> -->
        <div>
            <button type="submit">Modifier</button>
        </div>
    </div>
    
</form>

<%- include('./partials/footer') -%>